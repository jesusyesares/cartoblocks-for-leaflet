# Implementation Plans

Generated by the improve skill on 2026-06-12 (planned at commit `313481a`).
Execute in the order below unless dependencies say otherwise. Each executor:
read the plan fully before starting, honor its STOP conditions, and update
your row when done.

Goal: clear the path to a WordPress.org submission. Plans 001–002 are
release blockers. 003–008 are cheap (S effort) security/tooling hardening.
009–010 establish a first test suite.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Fix editor preview infinite reload loop with fitMarkers (issue #23) | P1 | S | — | DONE (commit `85908da` on `develop`; live browser smoke test via Playwright on a 2-marker/fitMarkers=true block confirmed `admin-ajax.php?action=bflm_preview` fires twice on load (505ms apart, initial mount + re-render) then zero times over 12s — no loop; map renders fitted to both markers, zero console/page errors) |
| 002  | Tag and release v1.0.7 | P1 | S | 001 | DONE — discovered `main` and `develop` had unrelated histories (different root commits, full history previously rebased onto `develop`, `main` never updated). Operator chose to force `main` to match `develop`'s history (`git push --force origin develop:main`); `origin/main` now at `85908da` (1.0.7, all 5 version locations confirmed). Tagged `v1.0.7` (annotated) and pushed. |
| 003  | Validate postMessage origin in block editor | P2 | S | — | DONE (commit `1d75fd6` on `develop`; live browser smoke test of preview iframe sync still recommended) |
| 004  | Use wp_safe_remote_get in geocoder | P2 | S | — | DONE (commit `d4d4a8c` on `develop`; live geocoding smoke test still recommended) |
| 005  | Validate overlay src with esc_url in shortcode builder | P2 | S | — | DONE (commit `cefaac7` on `develop`; live overlay render smoke test still recommended) |
| 006  | Expand PHPCS and PHPStan coverage to includes/ | P2 | S | — | DONE (commits `c1439ea`, `9f33494`, `f7ce0a9`, `6203145` on `develop`; `composer lint`/`composer phpstan` clean for `includes/` — remaining warnings/errors are the pre-existing, documented `render.php` issue, out of scope) |
| 007  | Pin @wordpress/* dependencies to semver ranges | P3 | S | — | DONE (commit `a3fd70b` on `develop`; `npm install`/`npm run build`/`npm test` (35/35) pass, resolved versions unchanged) |
| 008  | Regenerate the translation template (.pot) | P3 | S | — | DONE (commit `3c74004` on `develop`; 0.10.0 → 1.0.7, 264→250 msgids — net -14 verified as dead pre-refactor strings with zero source references, 0 strings lost; executor's literal STOP on "count not higher" overridden by reviewer after confirming this) |
| 009  | PHPUnit characterization tests for preview input sanitiser | P2 | M | — | DONE (commits `4deccd1`, `e77720f` on `develop`; PHPUnit 9.6 infra + 66 tests/229 assertions, all green via `composer test`; `includes/preview/input.php`/`includes/shortcodes/attrs.php` unchanged; zoomControl `'0'` quirk documented as as-designed, not a bug) |
| 010  | PHPUnit characterization tests for shortcode attribute normalisation | P2 | M | 009 | DONE (commit `b98b72f` on `develop`; +49 tests (115 total, 304 assertions), all green via `composer test`; `includes/shortcodes/attrs.php` unchanged; case 4's whitespace assumption corrected — `sanitize_text_field()` trims before the regex, documented as as-designed) |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- 002 should land after 001 — don't tag a release that still has the
  editor-breaking oscillation bug from issue #23.
- 010 depends on 009 because it reuses the PHPUnit bootstrap/test
  infrastructure (composer scripts, bootstrap file, WP stubs wiring) that
  009 sets up. If 009 is skipped, 010's "Commands you will need" section
  must be redone from scratch.
- 006 should ideally land before 009/010 land in CI, so the new test files
  in `includes/` are covered by PHPCS/PHPStan too — but 009/010 can be
  written and run standalone first.

## Findings considered and rejected

- Double `wp_kses_post()` on the `attribution` field (input.php:44 +
  map.php:46): confirmed but harmless — both calls are idempotent safe-HTML
  filters. Not worth a plan; if anyone touches this code, drop one of the
  two calls for clarity, but it's cosmetic.
- "marker.php missing null-safety on lat/lng before float cast"
  (`includes/shortcodes/marker.php`): false positive. `bflm_build_marker_shortcodes()`
  already guards with `if ( ! isset( $marker['lat'], $marker['lng'] ) ) { continue; }`
  before any cast. No action needed.
- Issue #27 (GPX from Media Library not rendering): root cause is the
  server's `Content-Type` for `.gpx` files / upstream Leaflet Map's
  `leaflet-ajax-geojson.js` expecting `responseXML`. Not a Blocks for
  Leaflet Map code bug. Recommended action (not planned here): add a "Known
  Limitations" note to readme.txt and/or file an issue upstream at
  bozdoz/wp-plugin-leaflet-map. Does not block WP.org submission.
- v1.2.0 edit.js modularization (7457 lines → smaller modules): real and
  worth doing, but it's a large refactor that should happen AFTER the
  WP.org submission to keep submission scope tight and low-risk. Candidate
  module seams identified for a future plan: shortcode-builder/URL-building
  helpers (~lines 95–994), iframe postMessage sync (~lines 1337–1700),
  marker/line/circle attribute handlers (~lines 1702–2488), geocoding UI
  (~lines 1161–1773), leaving a slimmer main edit component (~1500 lines).
